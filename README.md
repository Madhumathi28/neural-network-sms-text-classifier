# neural-network-sms-text-classifier
Classifies SMS messages as "ham" (normal) or "spam" (advertisements) using a Neural Network. The project preprocesses text data, trains a Keras model, and includes a predict_message() function that outputs the probability and label for new messages. Fully runnable in Google Colab.
